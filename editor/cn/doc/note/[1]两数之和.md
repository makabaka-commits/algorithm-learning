在用枚举法的时候，如果数组长度不确定，只能先输入数组长度然后才能输入，什么方法会更好的解决这一点呢
return一定要多想想，后面没有return的话不可以，程序无法接受
另一个方法哈希表,哈希表就是通过一个函数快速定位目标数字位置的工具，它面向对象必须是封装
Integer是int的封装形式
hashtable.containsKey();
hashtable.get();
hashtable.put();
输入的是数字，输出的是位置
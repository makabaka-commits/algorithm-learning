一个非常巧妙的方法，通过一个数组table去记录字母的出现频次，假如在s中它出现了频次加一，那么怎么和t对应呢，假如字母在t中出现，那么频次减一
这样的话，最终频次如果是小于0的说明有字母不一样，就返回false了。
我开始想的是在字符串里寻找字母，如果是说在哈希集合里假如能把字符插进去，说明有不一样的，返回flase。但是这样似乎无法执行。因为hashset只能判断有没有这个字符，但是hashmap可以判断个数。所以说不太行。
这个方法和哈希表有什么关系呢。
这个方法里的table本质上就是在用数组来模拟哈希表，哈希表本质是用一个key来找对应的value，也就是字母对应出现次数，但是由于字符串里只有小写字母，所以直接用一维数组就可以了，
代码里的charAt的用法是取出string里面指定位置的字符。
如果不只是小写字母的话应该用哈希表来
map.put(sc,map.getOrDefault(sc,o)+1)，这里的0是默认值，getordefault用来如果没有次数返回0，有次数返回次数
统计次数时就可以来使用这个方法
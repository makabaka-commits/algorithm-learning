这个题呢我的思路是差不多的，有些地方要再精进一些
首先我想用的是哈希表，用循环结构对给定数组中每个元素在哈希表中寻找它的键，如果键不等于循环的i，说明重复。这个思路需要改进的有，哈希表中每个元素的键是唯一的，在遍历属于nums的x的时候，其实才把元素键位和数字存进去，这个hashtable.containKey
是在看这个键存不存在，如果已经存在，那就重复返回ture了
还有一种方法是用Hashset，它和哈希表不一样的是，除了键唯一，元素也不能重复。而且可以使用的方法也不一样，
这个可以使用add，如果说加不进去一个元素说明重复。hashset是单参数，使用请注意
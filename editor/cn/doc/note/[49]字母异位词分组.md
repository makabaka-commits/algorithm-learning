我的方法虽然时间长了点，没有想到用表，思路还是非常对的。
定义表List<String> result =new ArrayList<>();因为数组是定长的，所以说不适合这个题目。表不是，表可以往里用add加，也可以用contain看是不是这个元素存在，数组就只能遍历，所以说表更适合，
除此以外还有一个点，假如说这个字符串已经存在在out里面，continue的作用是直接跳过下面的代码，我原本把这一步放在内循环的，其实放在外循环就可以了，因为eat和tea有一个和别的比较就可以了，他俩等价。
答案给的方法呢是用哈希表，用字母排序或者字母计数来作为键位，如果键位相同，字符串并到同一个表里，
计数那个通过的是StringBuffer这个方法，这个方法可以在字符串里添加东西，它把eat变成a1e1t1，再把加出来的这个用tostring返回字符串作为键，value是相同键位的表，同时这个方法也用到上一道题的map.getOrDefault
另外学习map.value
另一个方法给字符串字母排序，这个键怎么生成的呢
先把字符串转成字符数组char[] array = str.CharArray
Array.sort(array)是将字符按字母顺序排出来，然后再把这个数组转成字符串，String key = new String(array),其他的和另一个数组一样了。
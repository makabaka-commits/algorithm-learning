### 解题思路

转成数组 排序 转成字符串 比较字符串是否相等 简单明了

### 代码

* java

```java
class Solution {
    public boolean isAnagram(String s, String t) {
         char[] ch1 = s.toCharArray();
       char[] ch2 = t.toCharArray();
       Arrays.sort(ch1);
       Arrays.sort(ch2);
       s = new String(ch1);
       t = new String(ch2);
       if (s.equals(t)){
           return true;
       }
       return false;

    }
}
```

